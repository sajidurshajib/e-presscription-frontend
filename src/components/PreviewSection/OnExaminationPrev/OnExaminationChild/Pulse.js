const Pulse = ({ pulse }) => {
    return (
        <div>
            Pulse{' '}
            <span>
                <b>{pulse}</b> beats/min
            </span>
        </div>
    )
}

export default Pulse
